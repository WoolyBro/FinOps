# The model runs on Amazon Bedrock (boto3 ships with strands-agents).
# The gemini extra is the free-tier cloud path; ollama is the offline one.
strands-agents[ollama,gemini]>=1.54
strands-agents-tools>=0.8
python-dotenv>=1.0
reportlab>=4.0

# tests
pytest>=8.0
pypdf>=5.0

# AgentCore Runtime
bedrock-agentcore>=0.1

# Required to read credentials issued by `aws login`, which the CLI stores in a
# format the plain botocore credential chain cannot decrypt.
botocore[crt]>=1.43

# API
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
