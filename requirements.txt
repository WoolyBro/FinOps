# Claude runs on Amazon Bedrock (boto3 ships with strands-agents).
# The ollama extra is the credential-free local development path.
strands-agents[ollama]>=1.54
strands-agents-tools>=0.8
python-dotenv>=1.0
reportlab>=4.0

# tests
pytest>=8.0
pypdf>=5.0

# AgentCore Runtime
bedrock-agentcore>=0.1

# API
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
